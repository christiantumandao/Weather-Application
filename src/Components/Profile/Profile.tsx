import { userData } from "../../util/types"
type ProfileProps = {
    userData: userData
}

const Profile = (props : ProfileProps) => {
    return (
        <div>

        </div>
    )
}


export default Profile